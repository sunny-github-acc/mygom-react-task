export enum Routes {
  Login = "/login",
  Users = "/items",
  Weak = "/items/weak",
  Wrong = "/items/wrong",
  Reused = "/items/reused",
  Old = "/items/old",
  Root = "/",
}

export enum API {
  Login = "api/login",
  Items = "api/items",
  User = "api/user",
}
